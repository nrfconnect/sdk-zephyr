---
title: Nordic FICR (Factory Information Configuration)
version: 0.1

description: >
    Binding for the Nordic FICR (Factory Information Configuration)

properties:
    compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "nordic,nrf-ficr"
      generation: define

    reg:
      type: array
      description: mmio register space
      generation: define
      category: required

...
